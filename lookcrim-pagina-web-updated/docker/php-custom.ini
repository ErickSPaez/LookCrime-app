; Custom PHP settings for Cloud Run uploads
; Keep these aligned with Laravel validation (currently max 8192KB per image)

file_uploads=On
upload_max_filesize=12M
post_max_size=25M
max_file_uploads=10

; Sensible runtime defaults
memory_limit=256M
max_execution_time=120
max_input_time=120
